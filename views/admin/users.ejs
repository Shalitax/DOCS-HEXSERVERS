<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gestión de Usuarios - HexServers Admin</title>
  <link rel="icon" type="image/x-icon" href="/images/favicon.ico">
  <link href="/css/output.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body class="text-white min-h-screen">
  
  <header class="glass border-b border-white/20 shadow-lg">
    <div class="container mx-auto px-6 py-4">
      <div class="flex items-center justify-between">
        <h1 class="text-2xl font-bold font-display text-white">
          Gestión de Usuarios
        </h1>
        <div class="flex space-x-3">
          <button onclick="showUserModal()" class="bg-gradient-to-r from-green-500 to-emerald-500 px-4 py-2 rounded-lg hover:from-green-600 hover:to-emerald-600 transition-all text-white font-semibold">
            <i class="fas fa-plus mr-2"></i>Nuevo Usuario
          </button>
          <a href="/admin" class="glass px-4 py-2 rounded-lg hover:bg-white/20 transition-all font-medium">
            <i class="fas fa-arrow-left mr-2"></i>Volver
          </a>
        </div>
      </div>
    </div>
  </header>

  <div class="container mx-auto px-6 py-8">
    
    <!-- Tabla de usuarios -->
    <div class="glass rounded-xl border border-white/20 shadow-xl overflow-hidden">
      <table class="w-full">
        <thead class="bg-white/5">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Usuario</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Email</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-gray-400 uppercase tracking-wider">Creado</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-gray-400 uppercase tracking-wider">Acciones</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-white/10">
          <% users.forEach(user => { %>
            <tr class="hover:bg-white/5 transition-all">
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="flex items-center">
                  <div class="w-10 h-10 bg-purple-500/20 rounded-full flex items-center justify-center mr-3">
                    <i class="fas fa-user text-purple-400"></i>
                  </div>
                  <div class="text-sm font-medium text-white"><%= user.username %></div>
                </div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm text-gray-300"><%= user.email || 'No especificado' %></div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap">
                <div class="text-sm text-gray-400">
                  <%= new Date(user.created_at).toLocaleDateString('es-ES', { year: 'numeric', month: 'short', day: 'numeric' }) %>
                </div>
              </td>
              <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                <button onclick="editUser(<%= user.id %>, '<%= user.username %>', '<%= user.email || '' %>')" class="glass px-3 py-1 rounded hover:bg-blue-500/20 text-blue-400 transition-all mr-2">
                  <i class="fas fa-edit"></i>
                </button>
                <button onclick="deleteUser(<%= user.id %>, '<%= user.username %>')" class="glass px-3 py-1 rounded hover:bg-red-500/20 text-red-400 transition-all">
                  <i class="fas fa-trash"></i>
                </button>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
      
      <% if (users.length === 0) { %>
        <div class="p-8 text-center text-gray-400">
          No hay usuarios disponibles.
        </div>
      <% } %>
    </div>

  </div>

  <!-- Modal para Usuario -->
  <div id="userModal" class="fixed inset-0 bg-black/80 backdrop-blur-sm hidden flex items-center justify-center z-50">
    <div class="glass rounded-xl p-8 border border-white/20 max-w-md w-full mx-4 animate-slide-up shadow-2xl">
      <h2 id="userModalTitle" class="text-2xl font-bold mb-6 font-display">Nuevo Usuario</h2>
      <form id="userForm" class="space-y-4">
        <input type="hidden" id="userId">
        <div>
          <label class="block text-sm font-medium mb-2">Usuario</label>
          <input type="text" id="username" required class="w-full glass px-4 py-2 rounded-lg focus:ring-2 focus:ring-white text-white">
        </div>
        <div>
          <label class="block text-sm font-medium mb-2">Email</label>
          <input type="email" id="email" class="w-full glass px-4 py-2 rounded-lg focus:ring-2 focus:ring-white text-white">
        </div>
        <div>
          <label class="block text-sm font-medium mb-2">
            Contraseña 
            <span id="passwordLabel" class="text-gray-400 text-xs">(requerida)</span>
          </label>
          <input type="password" id="password" class="w-full glass px-4 py-2 rounded-lg focus:ring-2 focus:ring-white text-white">
        </div>
        <div class="flex space-x-3 mt-6">
          <button type="submit" class="flex-1 bg-gradient-to-r from-green-600 to-green-800 px-4 py-2 rounded-lg hover:from-green-700 hover:to-green-900 transition-all text-white font-semibold">
            Guardar
          </button>
          <button type="button" onclick="hideModal('userModal')" class="glass px-4 py-2 rounded-lg hover:bg-white/10 transition-all">
            Cancelar
          </button>
        </div>
      </form>
    </div>
  </div>

  <!-- Modal de Confirmación -->
  <div id="adminConfirmModal" class="fixed inset-0 bg-black/80 backdrop-blur-sm hidden items-center justify-center z-[60]">
    <div class="glass rounded-xl p-8 border border-white/20 max-w-md w-full mx-4 animate-slide-up shadow-2xl">
      <div id="adminConfirmIcon" class="mx-auto w-16 h-16 rounded-full flex items-center justify-center mb-4 bg-red-500/20">
        <i class="fas fa-exclamation-triangle text-2xl text-red-400"></i>
      </div>
      <p id="adminConfirmMessage" class="text-center text-white mb-6"></p>
      <div class="flex space-x-3">
        <button id="adminConfirmBtn" class="flex-1 bg-gradient-to-r from-red-500 to-red-600 px-4 py-2 rounded-lg hover:from-red-600 hover:to-red-700 transition-all text-white font-semibold">
          Confirmar
        </button>
        <button onclick="hideConfirmModal()" class="flex-1 glass px-4 py-2 rounded-lg hover:bg-white/10 transition-all">
          Cancelar
        </button>
      </div>
    </div>
  </div>

  <!-- Modal de Alerta -->
  <div id="adminAlertModal" class="fixed inset-0 bg-black/80 backdrop-blur-sm hidden items-center justify-center z-[60]">
    <div class="glass rounded-xl p-8 border border-white/20 max-w-md w-full mx-4 animate-slide-up shadow-2xl">
      <div id="adminAlertIcon" class="mx-auto w-16 h-16 rounded-full flex items-center justify-center mb-4 bg-green-500/20">
        <i class="fas fa-check-circle text-2xl text-green-400"></i>
      </div>
      <p id="adminAlertMessage" class="text-center text-white mb-6"></p>
      <div class="flex justify-center">
        <button onclick="hideAlertModal()" class="glass px-6 py-2 rounded-lg hover:bg-white/10 transition-all">
          Cerrar
        </button>
      </div>
    </div>
  </div>

  <script src="/js/admin-utils.js"></script>
  <script src="/js/admin-users.js"></script>

</body>
</html>
